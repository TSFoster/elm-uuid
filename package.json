{
  "scripts": {
    "test": "rm -rf tests/VerifyExamples && elm-format --validate . && elm-verify-examples && elm-test"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-test": "^0.19.1",
    "elm-verify-examples": "^5.0.0"
  }
}
